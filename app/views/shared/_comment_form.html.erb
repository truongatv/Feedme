<div class="comment-form">
	<%=form_for(@image_comment, remote: true) do |f| %>
		<div class="field">
			<%= f.text_field :comment, class: "comment-text-field", placeholder: "Say something"%>
			<%= f.hidden_field :image_id, :value => image.id %>
		</div>
	<% end %>		
</div>